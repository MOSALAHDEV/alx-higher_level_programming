-- Task: Create a database only if does not already exist.
-- The following statement creates the database named 'hbtn_0c_0'
-- only if it does not already exist.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
