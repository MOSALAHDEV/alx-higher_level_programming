-- This script creates a table called "first_table" if it doesn't exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT,          -- Define an integer column "id"
    name VARCHAR(256) -- Define a varchar column "name"
);
